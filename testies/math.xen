export method add(a: int, b: int) -> int {
    release a + b
}

export method multiply(a: int, b: int) -> int {
    release a * b
}

export let PI: float = 3.14159

# Private function
method _helper(x: int) -> int {
    release x * 2
}
